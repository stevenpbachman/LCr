# Generate the assessments.csv file

Generate the assessments.csv file

## Usage

``` r
sis_assessments(unique_id, native_ranges, wcvp_ipni_id, occs)
```

## Arguments

- unique_id:

  (character) Unique identifier.

## Value

Returns an SIS compliant data frame
