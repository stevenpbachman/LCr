# Generate the allfields.csv file

Generate the allfields.csv file

## Usage

``` r
sis_allfields(unique_id, occs = NULL)
```

## Arguments

- unique_id:

  (character) Unique identifier.

## Value

Returns an SIS compliant data frame
